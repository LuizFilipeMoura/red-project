{
  "name": "@repo/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "lint": "eslint . --ext .ts",
    "test": "jest",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "db:drizzle:migrate": "drizzle-kit generate:sqlite && drizzle-kit up:sqlite"
  },
  "dependencies": {
    "@prisma/client": "^6.17.1",
    "better-sqlite3": "^9.4.3",
    "drizzle-orm": "^0.30.10",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "drizzle-kit": "^0.21.4",
    "prisma": "^6.17.1",
    "typescript": "^5.4.5"
  }
}
